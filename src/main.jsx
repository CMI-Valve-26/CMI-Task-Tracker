import React from 'react'
import ReactDOM from 'react-dom/client'
import CMITracker from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CMITracker />
  </React.StrictMode>
)
